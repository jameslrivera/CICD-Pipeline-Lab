apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "phishing-detector.fullname" . }}-config
  labels:
    {{- include "phishing-detector.labels" . | nindent 4 }}
data:
  detector.yaml: |
    threshold: {{ .Values.detector.threshold }}
