apiVersion: v1
kind: Service
metadata:
  name: phishing-detector
  namespace: phishing-detector
  labels:
    app: phishing-detector
spec:
  type: ClusterIP
  selector:
    app: phishing-detector
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
