# Test and lint tooling. Deliberately NOT installed into the runtime image —
# the smaller the shipped image, the smaller the attack surface.
-r requirements.txt
pytest==8.3.4
httpx==0.28.1
ruff==0.8.6
